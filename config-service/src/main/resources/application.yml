server:
  port: 8888
spring:
  cloud:
    config:
      server:
        git:
          default-label: config-server
          uri: https://github.com/juanpabloardilama/micro-service-tutorial
          search-paths: config-data
  application:
    name: config-service
