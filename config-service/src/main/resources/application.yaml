server:
  port: 8080
spring:
  cloud:
    config:
      server:
        git:
          default-label: config-server
          uri: https://github.com/cavanosa/microservices-tutorial
          search-path: config-data
  application:
    name: config-service
